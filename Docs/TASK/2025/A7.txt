FelipedelosH

Problema: Los textos que se muestran en el menú de pausa se muestra el nombre del estado... cambiar a un texto.

__________________________________________________________________________________________
Para ello se creo: 

"config/config.json"
"assets/LAN/ESP/TEXT.json"
"src/UI/TkinterRenderer.py"

la cual resuelve:

"config.json" allí se configura un lenguaje... y se optienen los textos como "text_xyz"
y esto se hace en la implementación del contrato de pintura "TkinterRenderer" >> render_game_pause

Se hace a traves de la invocación del servicio de configuración:
    * self.configuration.get("text_xyz")

Dependencias:

* Json.
* Config.
* States machines.
