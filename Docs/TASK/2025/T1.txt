FelipedelosH

Problema: Se necesita un estado de pausa del juego para mostrar textos
al jugador, se sabe que hay que crear estados y código en muchas partes.
__________________________________________________________________________________________
Para ello se creo: 

"config/config.json"
"src/core/controller.py"
"src/core/gameStateManager.py"
"src/core/gameState.py"
"src/core/inputHandler.py"
"src/UI/UIManager.py"
"src/UI/IUIRenderer.py"
"src/UI/TkinterRenderer.py"

la cual resuelve:

Existe una máquina de estados registrada en: "statesMachines.game" allí se encuentran los estados posibles de juego...
Hay que añadir un nuevo estado:
gameTextDisplayed

Y ese estado tiene que tener una transición vamos a tomar de ejemplo:

Para el inputHandler existen 2 estados que vamos a tomar de ejemplo:
if _pivot == "gameStart":
elif _pivot == "gamePause":

Vamos a incluir el nuevo...

Todo empieza en el controller en donde existen los imports gameState.
Tenemos que importar el nuevo. Luego agregarlo al ARR de estados.

El evento que dispara el cambio de estado se hace en inputHandler con el método:
self.gameStateManager.getStateMachine("game").mouvePointer(self.control.key_?)

Y luego de ello tenemos que declarar un contrato para renderizar el nuevo estado:
"IUIRenderer"

Y ese contrato se implementará en:
"TkinterRenderer"

Y se invocará desde: "UIManager.showGameTextDisplayed()"

Pero su orquestación se hace desde: "gameState.GameTextDisplayedState.render()"
Y ese contro se va hacia: main.render

Dependencias:

* Configuración.
* Handlers.
* State Machines.


OJO SE quito esto del config.json:
        "gameStart:gameStart:controls:action_buttons",